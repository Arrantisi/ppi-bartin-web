# Structure

This folder contains the documented target structure for the app rebuild.

- Routes live in `app/`
- Domain features live in `features/`
- Shared UI lives in `components/`
- Server behavior lives in `server/`
- Infrastructure adapters live in `lib/`
