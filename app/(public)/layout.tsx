import { Metadata } from "next";

export const metadata: Metadata = {
  title: "PPI Bartin | Login",
  description: "Generated by create next app",
};

const PublicLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default PublicLayout;
